@model ProfileModel
@{ //only test data in here for now, change it later so it works
                
    ViewData["Title"] = "Profile Page"; 
}
@if (User.IsInRole("Guest"))
{
    @Html.DisplayForModel("Profile")
    
    @if (User.IsInRole("Admin"))
    {
        @Html.DisplayForModel("ProfileForAdmin")
    }
}

